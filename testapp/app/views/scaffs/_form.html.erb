<%= form_for(@scaff) do |f| %>
  <% if @scaff.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scaff.errors.count, "error") %> prohibited this scaff from being saved:</h2>

      <ul>
      <% @scaff.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :article %><br>
    <%= f.text_area :article %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
